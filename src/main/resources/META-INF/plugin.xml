<idea-plugin>
    <id>com.chelayel.geminirelay</id>
    <name>Gemini Relay</name>
    <vendor email="charbelhelayel@gmail.com" url="https://github.com/Chelayel/gemini-relay">Chelayel</vendor>

    <description><![CDATA[
        <h2>Gemini Relay — Google Gemini, native in your JetBrains IDE</h2>
        <p>A native chat panel that brings <b>Google Gemini</b> into IntelliJ IDEA, PyCharm, WebStorm,
        GoLand, Rider and the rest of the JetBrains family — a side-panel coding assistant that talks
        directly to the Gemini API, to Vertex AI, or to a Vertex deployment behind a custom Apigee
        gateway, without leaving your IDE or living in the terminal.</p>

        <p><b>Highlights</b></p>
        <ul>
            <li><b>Streaming chat</b> with live tool activity — edits, commands, and file reads as they happen</li>
            <li><b>Agent &amp; Ask modes</b> — let Gemini edit and run commands, or keep it strictly read-only</li>
            <li><b>Editor-aware</b> — auto-attaches your current selection, so "explain/refactor this" just works</li>
            <li><b>Attach files &amp; images</b> — pick a file, or paste a screenshot straight into the prompt (Gemini is multimodal)</li>
            <li><b>Three connection modes</b> — Gemini API key, Vertex AI, or Vertex via Apigee</li>
            <li><b>Configurable everything</b> — connection parameters in Settings; secrets in the IDE password safe</li>
        </ul>

        <p>Configure the connection under <b>Settings → Tools → Gemini Relay</b>.</p>

        <p>☕ <b>Free and open source</b> (MIT). Source, issues, and contributions:
        <a href="https://github.com/Chelayel/gemini-relay">github.com/Chelayel/gemini-relay</a>.</p>

        <p><i>Independent and unofficial — not affiliated with Google. Part of the “Relay” family of
        IDE companions by Chelayel.</i></p>
    ]]></description>

    <change-notes><![CDATA[
        <p><b>1.1.0</b> — Current models by default. New chats now use
        <code>gemini-3.7-flash</code>, Google's current model for coding and agentic work,
        instead of <code>gemini-2.5-pro</code>, and the suggested list follows the 3.x lineup.
        The list also matches the connection mode — Vertex names some models differently from
        the Gemini API, and a selection that has an equivalent id is translated when you switch
        modes rather than left to fail with a 404. In Gemini API mode the live model list is
        sorted newest-generation first and no longer offers models this panel can't drive
        (embeddings, TTS, Live, image and video generation).</p>
        <p><b>1.0.9</b> — The 1.0.8 fix for <code>SimpleListCellRenderer</code> swapped one
        scheduled-for-removal overload for another (both static factories are marked for
        removal). Replaced all 4 call sites with the current <code>textListCellRenderer</code>
        DSL instead, which carries no deprecation.</p>
        <p><b>1.0.8</b> — Cleared plugin-verifier warnings: replaced the scheduled-for-removal
        <code>SimpleListCellRenderer.create(String, Function)</code> overload with its
        <code>Customizer</code> replacement (4 call sites), and made the credential-attributes
        lookup use the explicit two-argument constructor.</p>
        <p><b>1.0.6</b> — Refreshed the model list for Google's current lineup and stopped
        requests failing on retired models: a saved model Google has shut down (e.g.
        <code>gemini-3-pro-preview</code>) now auto-migrates to a current default, and the
        error shown when a model is gone points you to the model selector.</p>
        <p><b>1.0.5</b> — Restored the rich chat transcript on IntelliJ 2026.2+ (build 262),
        where JCEF became a separate bundled plugin — the panel now depends on it optionally
        so the in-browser view works again instead of dropping to plain text. The plain-text
        fallback also renders at the correct font size when JCEF isn't available.</p>
        <p><b>1.0.3</b> — Fixed the tool window failing to open on recent IntelliJ builds
        (<code>NoClassDefFoundError: com/intellij/ui/jcef/JBCefApp</code>): it now degrades to
        the plain-text transcript instead of failing to initialize.</p>
        <p><b>1.0.0</b> — First release: native Google Gemini chat panel with streaming
        transcript &amp; tool activity, Agent &amp; Ask modes, three connection modes
        (Gemini API, Vertex AI, Vertex via Apigee), editor-aware context, file &amp; image
        attachments, personas, project memory, and MCP tool servers.</p>
    ]]></change-notes>

    <depends>com.intellij.modules.platform</depends>
    <!-- JCEF moved into a bundled plugin in 2026.2+ (build 262). Depend on it
         optionally so the rich in-browser transcript is available where JCEF
         exists, while the plugin still loads (and falls back to the plain-text
         transcript) on IDEs/environments without it. -->
    <depends optional="true" config-file="jcef.xml">com.intellij.modules.jcef</depends>

    <extensions defaultExtensionNs="com.intellij">
        <applicationService serviceImplementation="com.chelayel.geminirelay.settings.GeminiSettings"/>

        <applicationConfigurable
            parentId="tools"
            instance="com.chelayel.geminirelay.settings.GeminiSettingsConfigurable"
            id="com.chelayel.geminirelay.settings"
            displayName="Gemini Relay"/>

        <toolWindow
            id="Gemini Relay"
            anchor="right"
            secondary="false"
            icon="/icons/gemini.svg"
            factoryClass="com.chelayel.geminirelay.ui.GeminiToolWindowFactory"/>
    </extensions>
</idea-plugin>
